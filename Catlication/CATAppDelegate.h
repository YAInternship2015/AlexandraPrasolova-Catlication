
#import <UIKit/UIKit.h>

#warning не понял, куда пропала шапка с копирайтом во всех файлах? ее не нужно удалять если что )
#warning проект кстати не собирался из-за добавленого файла about.txt, который Вы удалили физически, но в проекте он остался прилинкован
@interface CATAppDelegate : UIResponder <UIApplicationDelegate>

@property (strong, nonatomic) UIWindow *window;

@end

